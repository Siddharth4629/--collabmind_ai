# Step 6 — replace with your actual backend URL (no trailing slash)
VITE_API_URL=https://collabmind-backend.onrender.com

# Local dev: comment out VITE_API_URL so Vite proxies /api and sockets to localhost:5000
